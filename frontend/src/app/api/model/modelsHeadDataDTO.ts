

// TODO: this must be stored and created in the backend
export interface ModelsEmployerRatingDTO {
  agreedOn?: number;
  name?: string;
  abstract?: string;
  experienceSince?: Date;
  startAtProdyna?: Date;
  age?: number;
}

